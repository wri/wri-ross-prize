{#
/**
 * @file
 * Default theme implementation for an 'input' #type form element.
 *
 * Available variables:
 * - attributes: A list of HTML attributes for the input element.
 * - children: Optional additional rendered elements.
 *
 * @see template_preprocess_input()
 *
 * @ingroup themeable
 */
#}
<div class="input">
    <input{{ attributes.setAttribute('id', 'additional_information') }} />
    <input type="text" value="" />
</div>
<p class="files-error-note" style="display: none;"></p>
<p class="field-note">To select multiple files, hold CTRL (CMD) or SHIFT.</p>